---

title: "简单类型之字符串"
date: 2019-10-25T09:00:00
lastmod: 2019-10-25T09:00:00
timezone: UTC+8
draft: false
tags: ["simple types", "Code"]
categories: ["Code"]
author: "zyscn"

---
# Strings
## Change the case of characters
In computer programming, a [string](https://en.wikipedia.org/wiki/String_(computer_science)) is traditionally a sequence of characters, either as a literal constant or as some kind of variable. The latter may allow its elements to be mutated and the length changed, or it may be fixed (after creation). A string is generally considered as a data type and is often implemented as an array data structure of bytes (or words) that stores a sequence of elements, typically characters, using some character encoding. String may also denote more general arrays or other sequence (or list) data types and structures.

```python
str = "Lower and upper"  
  
lower = str.lower()  
# lower is "lower and upper"  
  
upper = str.upper()  
# upper is "LOWER AND UPPER"
```

```java
String str = "Lower and upper";  
  
String lower = str.toLowerCase();  
//lower is "lower and upper"  
  
String upper = str.toUpperCase();  
//upper is "LOWER AND UPPER"
```

```javascript
var str = "Lower and upper";  
  
var lower = str.toLowerCase();  
//lower is "lower and upper"  
  
var upper = str.toUpperCase();  
//upper is "LOWER AND UPPER"
```

```cs
var str = "Lower and upper";  
  
var lower = str.ToLower();  
//lower is "lower and upper"  
  
var upper = str.ToUpper();  
//upper is "LOWER AND UPPER"
```

```go
var str = "Lower and upper"  
  
var lower = strings.ToLower(str)  
//lower is "lower and upper"  
  
var upper = strings.ToUpper(str)  
//upper is "LOWER AND UPPER"
```
## Escaping characters

```python
# \t Insert a tab.  
# \n Insert a newline.  
# \r Insert a carriage return.  
# \' or ' Insert a single quote.  
# \" Insert a double quote.  
# \\ or \ Insert a backslash character.  
  
str = "She said \"Hello!\" to me."  
# str is "She said "Hello!" to me."
```

```java
// \t Insert a tab.  
// \b Insert a backspace.  
// \n Insert a newline.  
// \r Insert a carriage return.  
// \' or ' Insert a single quote.  
// \" Insert a double quote.  
// \\ Insert a backslash character.  
  
String str = "She said \"Hello!\" to me.";  
//str is "She said "Hello!" to me."
```

```javascript
// \t Insert a tab.  
// \b Insert a backspace.  
// \n Insert a newline.  
// \r Insert a carriage return.  
// \' or ' Insert a single quote.  
// \" Insert a double quote.  
// \\ Insert a backslash character.  
  
var str = "She said \"Hello!\" to me.";  
//str is "She said "Hello!" to me."
```

```cs
// \t Insert a tab.  
// \b Insert a backspace.  
// \n Insert a newline.  
// \r Insert a carriage return.  
// \' or ' Insert a single quote.  
// \" Insert a double quote.  
// \\ Insert a backslash character.  
  
var str = "She said \"Hello!\" to me.";  
//str is "She said "Hello!" to me."
```

```go
// \t Insert a tab.  
// \b Insert a backspace.  
// \n Insert a newline.  
// \r Insert a carriage return.  
// ' Insert a single quote.  
// \" Insert a double quote.  
// \\ Insert a backslash character.  
  
var str = "She said \"Hello!\" to me."  
//str is "She said "Hello!" to me."
```
## Removing spaces
```python
s = " Spaces "  
  
trimS = s.strip()  
# trimS is "Spaces"
```

```java
String str = " Spaces ";  
  
String trimStr = str.trim();  
//trimStr is "Spaces"
```

```javascript
var str = " Spaces ";  
  
var trimStr = str.trim();  
//trimStr is "Spaces"
```

```cs
var str = " Spaces ";  
  
var trimStartStr = str.TrimStart();  
//trimStartStr is "Spaces "  
  
var trimEndStr = str.TrimEnd();  
//trimEndStr is " Spaces"  
  
var trimStr = str.Trim();  
//trimStr is "Spaces"
```

```go
var str = " Spaces "  
  
var trimStr = strings.TrimSpace(str)  
//trimStr is "Spaces"
```
## Substring index
```python
dataString = "Substring index"  
index = dataString.find("string")  
# index is 3
```

```java
String dataString = "Substring index";  
int index = dataString.indexOf("string");  
// index is 3
```

```javascript
var dataString = "Substring index";  
var index = dataString.indexOf("string");  
// index is 3
```

```cs
var dataString = "Substring index";  
var index = dataString.IndexOf("string");  
// index is 3
```

```go
var dataString = "Substring index"  
var index = strings.Index(dataString, "string")  
// index is 3
```
## converting to a number

```python
from locale import *  
  
# to Int  
strNumber = "42"  
number = int(strNumber)  
  
# to Double and Float  
# the first method  
strPi = "3.14"  
pifloat = float(strPi)  
  
# the second method  
strExp = "2.71828"  
exp = atof(strExp)  
  
# the third method  
strHalf = "0,5"  
half = atof(strHalf.replace(",", "."))
```

```java
//to int  
String strNumber = "42";  
//the first method  
int number = Integer.parseInt(strNumber);  
//the second method  
number = Integer.valueOf(strNumber);  
  
//to Double and Float  
//the first method  
String strPi = "3.14";  
float pi = Float.parseFloat(strPi);  
  
//the second method  
String strExp = "2.71828";  
double exp = Double.valueOf(strExp);  
  
//the third method  
String strHalf = "0,5";  
DecimalFormat formatter = new DecimalFormat();  
DecimalFormatSymbols sfs = new DecimalFormatSymbols();  
sfs.setDecimalSeparator(',');  
formatter.setDecimalFormatSymbols(sfs);  
double half = 0;  
try {  
half = formatter.parse(strHalf).doubleValue();  
} catch (ParseException e) {  
e.printStackTrace();  
}
```

```javascript
//to int  
var strNumber = "42";  
//the first method  
var number = Number(strNumber);  
//the second method  
number = parseInt(strNumber);  
  
//to Double and Float  
//the first method  
var strPi = "3.14";  
var pi = parseFloat(strPi);  
  
//the second method  
var strExp = "2.71828";  
var exp = +strExp;  
  
//the third method  
var strHalf = "0,5";  
var half = +strHalf.replace(",", ".");
```

```cs
//to Int  
var strNumber = "42";  
//the first method  
int number = int.Parse(strNumber);  
//the second method  
number = Convert.ToInt32(strNumber);  
  
//to Double and Float  
//the first method  
var strPi = "3.14";  
float pi = float.Parse(strPi);  
  
//the second method  
var strExp = "2.71828";  
var exp = Convert.ToDouble(strExp);  
  
//the third method  
var strHalf = "0,5";  
var formatter = new  CultureInfo("en");  
formatter.NumberFormat.NumberDecimalSeparator = ",";  
var half = Convert.ToDouble(strHalf, formatter);
```

```go
//to int  
var strNumber = "42"  
//the first method  
number1, err := strconv.Atoi(strNumber)  
  
//the second method  
number2, err := strconv.ParseInt(strNumber, 10, 16)  
  
//to Double and Float  
//the first method  
var strPi = "3.14"  
pi, err := strconv.ParseFloat(strPi, 64)  
  
//the second method  
var strHalf = "0,5"  
half1, err := strconv.ParseFloat(strHalf, 32)  
//half1 is 0  
if err != nil {  
fmt.Println(err)  
}  
strHalf = strings.Replace(strHalf, ",", ".", 1)  
half2, err := strconv.ParseFloat(strHalf, 32)  
//half2 is 0.5
```
## Substring inserting

```python
dataString = "string"  
  
dataString = "Sub" + dataString  
# dataString is "Substring"  
  
dataString = dataString + "!"  
# dataString is "Substring!"  
  
dataString = dataString[:9] + " inserting" + dataString[9:]  
# dataString is "Substring inserting!"
```

```java
String dataString = "string";  
  
StringBuilder sb = new StringBuilder(dataString);  
sb.insert(0, "Sub");  
dataString = sb.toString();  
//dataString is "Substring"  
  
sb.insert(9, "!");  
dataString = sb.toString();  
//dataString is "Substring!"  
  
sb.insert(9, " inserting");  
dataString = sb.toString();  
//dataString is "Substring inserting!"
```

```javascript
var dataString = "string";  
  
//first method:  
dataString = "Sub" + dataString;  
//dataString is "Substring"  
  
dataString = dataString + "!";  
//dataString is "Substring!"  
  
var pos = 9;  
dataString = dataString.slice(0, pos) +  
" inserting" + dataString.slice(pos);  
//dataString is "Substring inserting!"  
  
//second method:  
dataString = dataString.insert(0, "Sub");  
//dataString is "Substring"  
  
dataString = dataString.insert(9, "!");  
//dataString is "Substring!"  
  
dataString = dataString.insert(9, " inserting");  
//dataString is "Substring inserting!"  
  
document.writeln(dataString + "<br/>");  
  
String.prototype.insert = function (index, string) {  
return this.slice(0, index) +  
string + this.slice(index, this.length);  
};
```

```cs
var dataString = "string";  
  
dataString = dataString.Insert(0, "Sub");  
//dataString is "Substring"  
  
dataString = dataString.Insert(9, "!");  
//dataString is "Substring!"  
  
dataString = dataString.Insert(9, " inserting");  
//dataString is "Substring inserting!"
```

```go
var dataString = "string"  
  
dataString = "Sub" + dataString  
// dataString is "Substring"  
  
dataString = dataString + "!"  
// dataString is "Substring!"  
  
dataString = dataString[:9] +  
" inserting" + dataString[9:]  
// dataString is "Substring inserting!"
```
## Substring replacement

```python
startString = "3, 2, 1, go!";  
startString = startString\  
.replace("1", "one")\  
.replace("2", "two")\  
.replace("3", "three");  
# startString = "three, two, one, go!"
```

```java
String startString = "3, 2, 1, go!";  
startString = startString  
.replace("1", "one")  
.replace("2", "two")  
.replace("3", "three");  
//startString = "three, two, one, go!"
```

```javascript
var startString = "3, 2, 1, go!";  
startString = startString  
.replace("1", "one")  
.replace("2", "two")  
.replace("3", "three");  
//startString = "three, two, one, go!"  
  
//replace all  
var oneString = "1 1 1";  
oneString = oneString  
.replace(new RegExp("1", "g"), "one");  
//oneString is "one one one"
```

```cs
var startString = "3, 2, 1, go!";  
startString = startString  
.Replace("1", "one")  
.Replace("2", "two")  
.Replace("3", "three");  
//startString = "three, two, one, go!"
```

```go
var startString = "3, 2, 1, go!"  
startString = strings.  
Replace(startString, "1", "one", 1)  
startString = strings.  
Replace(startString, "2", "two", 1)  
startString = strings.  
Replace(startString, "3", "three", 1)  
//startString = "three, two, one, go!"  
  
//replace all  
var oneString = "1 1 1"  
oneString = strings.  
Replace(oneString, "1", "one", -1)  
//oneString is "one one one"
```
## Substring searching

```python
dataString = "Substring search"  
  
if "string" in dataString:  
print("dataString contain \"string\"")  
  
if dataString.startswith("Sub"):  
print("dataString starts with \"Sub\"")  
  
if dataString.endswith("search"):  
print("dataString ends with \"search\"")
```

```java
String dataString = "Substring search";  
if (dataString.contains("string")) {  
//dataString contain "string"  
}  
if (dataString.startsWith("Sub")) {  
//dataString starts with "Sub"  
}  
if (dataString.endsWith("search")) {  
//dataString ends with "search"  
}
```

```javascript
var dataString = "Substring search";  
  
//if (dataString.includes("string")) in ES6  
if (dataString.indexOf("string") !== -1) {  
//dataString contain "string"  
}  
if (dataString.indexOf("Sub") === 0) {  
//dataString starts with "Sub"  
}  
var search = "search";  
if (dataString.indexOf(search) === dataString.length - search.length) {  
//dataString ends with "search"  
}
```

```cs
var dataString = "Substring search";  
if (dataString.Contains("string")) {  
//dataString contain "string"  
}  
if (dataString.StartsWith("Sub")) {  
//dataString starts with "Sub"  
}  
if (dataString.EndsWith("search")) {  
//dataString ends with "search"  
}
```

```go
var dataString = "Substring search"  
if strings.Contains(dataString, "string") {  
fmt.Println("contain \"string\"")  
}  
if strings.HasPrefix(dataString, "Sub") {  
fmt.Println("starts with \"Sub\"")  
}  
if strings.HasSuffix(dataString, "search") {  
fmt.Println("ends with \"search\"")  
}
```
## Strings list concatenating

```python
numbers = ["one", "two", "three"]  
numberList = "; ".join(numbers)  
# numberList is "one; two; three"
```

```java
String[] numbers = {"one", "two", "three"};  
String numberList = String.join("; ", numbers);  
//numberList is "one; two; three"
```

```javascript
var numbers = ["one", "two", "three"];  
var numberList = numbers.join("; ");  
//numberList is "one; two; three"
```

```cs
var numbers = new[] {"one", "two", "three"};  
var numberList = string.Join("; ", numbers);  
//numberList is "one; two; three"
```

```go
var numbers = []string{"one", "two", "three"}  
var numberList = strings.Join(numbers, "; ")  
//numberList is "one; two; three"
```
## Strings interpolating

```python
fontSize = 14  
fontFamile = "Arial"  
style1 = "font-size: {0};font-family: \"{1}\"".format(  
fontSize, fontFamile)  
# style1 is "font-size: 14;font-family: "Arial""  
  
# method for version 3.6  
style2 = f"font-size: {fontSize};font-family: \"{fontFamile}\""  
# style2 is "font-size: 14;font-family: "Arial""
```

```java
int fontSize = 14;  
String fontFamile = "Arial";  
String style = String.format(  
"font-size: %d;font-family: %s",  
fontSize, fontFamile);  
//style is "font-size: 14;font-family: Arial"
```

```javascript
//Since ES6  
let fontSize = 14;  
let fontFamile = "Arial";  
let style = `font-size: ${fontSize};` +  
`font-family: ${fontFamile}`;  
//style is "font-size: 14;font-family: Arial"
```

```cs
var fontSize = 14;  
var fontFamile = "Arial";  
var style = string.Format(  
"font-size: {0};font-family: \"{1}\"",  
fontSize, fontFamile);  
//style is "font-size: 14;font-family: "Arial""
```

```go
var fontSize = 14  
var fontFamile = "Arial"  
var style = fmt.Sprintf("font-size: %v;font-family: %v",  
fontSize, fontFamile)  
//style is "font-size: 14;font-family: Arial"
```
## Strings comparision

```python
first = "A"  
second = "B"  
third = str('A')  
  
areEqual1 = first == second  
# areEqual1 is False  
  
areNotEqual = first != second  
# areNotEqual is True  
  
areEqual2 = first == third  
# areEqual2 is True
```

```java
String first = "A";  
String second = "B";  
String third = new String("A");  
  
Boolean areEqual = first.equals(third);  
//areEqual is true  
  
areEqual = first == third;  
//areEqual is false  
  
Boolean areNotEqual = !first.equals(second);  
//areNotEqual is true
```

```javascript
var first = "A";  
var second = "B";  
var third = 'A';  
  
var areEqual1 = first === second;  
//areEqual1 is false  
  
var areNotEqual = first !== second;  
//areNotEqual is true  
  
var areEqual2 = first.localeCompare(third) === 0;  
//areEqual2 is true
```

```cs
var first = "A";  
var second = "B";  
var third = new String(new [] {'A'});  
  
var areEqual = first == second;  
//areEqual is false  
  
var areNotEqual = first != second;  
//areNotEqual is true  
  
areEqual = first == third;  
//areEqual is true
```

```go
var first = "A"  
var second = "B"  
var third = "A"  
  
var areEqual1 = first == second  
//areEqual1 is false  
  
var areNotEqual = first != second  
//areNotEqual is true  
  
var areEqual2 = strings.Compare(first, third) == 0  
//areEqual2 is true
```
## Strings concatenating

```python
s1 = "three"  
s2 = "two"  
s3 = s1 + ", " + s2  
s3 += ", one"  
sGo = s3 + ", " + "go!";  
# sGo is "three, two, one, go!"
```

```java
String s1 = "three";  
String s2 = "two";  
String s3 = s1 + ", " + s2;  
s3 += ", one";  
String sGo = s3.concat(", ").concat("go!");  
//sGo is "three, two, one, go!"
```

```javascript
var s1 = "three";  
var s2 = "two";  
var s3 = s1 + ", " + s2;  
s3 += ", one";  
var sGo = s3 + ", " + "go!";  
//sGo is "three, two, one, go!"
```

```cs
var s1 = "three";  
var s2 = "two";  
var s3 = s1 + ", " + s2;  
s3 += ", one";  
var sGo = string.Concat(s3, ", ", "go!");  
//sGo is "three, two, one, go!"
```

```go
var s1 = "three"  
var s2 = "two"  
var s3 = "one"  
  
//first method  
var sGo1 = s1 + ", " + s2 + ", "  
sGo1 += s3  
sGo1 = strings.Join([]string{sGo1, ", ", "go!"}, "")  
//sGo1 is "three, two, one, go!"  
  
//second method (efficient way)  
var b strings.Builder  
b.WriteString(s1)  
b.WriteString(", ")  
b.WriteString(s2)  
b.WriteString(", ")  
b.WriteString(s3)  
b.WriteString(", go!")  
var sGo2 = b.String()  
//sGo2 is "three, two, one, go!"
```
## Character replacement

```python
str = "1-3-2"  
# str[2] = "2" <- Error  
  
lStr = list(str)  
lStr[2] = "2"  
lStr[4] = "3"  
str = "".join(lStr)  
# str is "1-2-3"
```

```java
String str = "1-3-2";  
//str[2] = '2'; //<-Error  
  
StringBuilder sb = new StringBuilder(str);  
sb.setCharAt(2, '2');  
sb.setCharAt(4, '3');  
str = sb.toString();  
//str is "1-2-3"
```

```javascript
var str = "1-3-2";  
var arStr = str.split("");  
arStr[2] = '2';  
arStr[4] = '3';  
str = arStr.join("");  
//str is "1-2-3"
```

```cs
var str = "1-3-2";  
//str[2] = '2'; //<-Error  
  
var sb = new  StringBuilder(str);  
sb[2] = '2';  
sb[4] = '3';  
str = sb.ToString();  
//str is "1-2-3"
```

```go
var str = "1-3-2"  
var arrStr = strings.Split(str, "")  
arrStr[2] = "2"  
arrStr[4] = "3"  
str = strings.Join(arrStr, "")  
//str is "1-2-3"
```
## Characters count

```python
def reverse(word):  
# Characters count  
charCount = len(word)  
  
i = charCount  
result = ""  
while i > 0:  
i = i - 1  
result += word[i]  
  
return result  
  
stringReverse = reverse("string")  
# stringReverse is "gnirts"
```

```java
String Reverse(String word) {  
//Characters count  
int charCount = word.length();  
String result = "";  
for (int i = charCount - 1; i >= 0; i--)  
{  
result += word.charAt(i);  
}  
return result;  
}  
  
String stringReverse = Reverse("string");  
//stringReverse = "gnirts"
```

```javascript
function Reverse(word) {  
//Characters count  
var charCount = word.length;  
var result = "";  
for (var i = charCount - 1; i >= 0; i--)  
{  
result += word[i];  
}  
return result;  
}  
  
var stringReverse = Reverse("string");  
//stringReverse = "gnirts"
```

```cs
string Reverse(string word) {  
//Characters count  
var charCount = word.Length;  
var result = "";  
for (var i = charCount - 1; i >= 0; i--)  
{  
result += word[i];  
}  
return result;  
}  
  
var stringReverse = Reverse("string");  
//stringReverse = "gnirts"
```

```go
func Reverse(word string) string {  
//Characters count  
var charCount = len(word)  
var result = ""  
for i := charCount - 1; i >= 0; i-- {  
result += string(word[i])  
}  
return result  
}  
  
var stringReverse = Reverse("string")  
//stringReverse = "gnirts"
```
## Split into a array

```python
strData = "1981|Kim Victorya|engineer"  
arrData = strData.split("|")  
year = int(arrData[0])  
# year is 1981  
name = arrData[1]  
# name is "Kim Victorya"  
position = arrData[2]  
# position is "engineer"
```

```java
String strData = "1981|Kim Victorya|engineer";  
String[] arrData = strData.split("\\|");  
int year = Integer.parseInt(arrData[0]);  
//year is 1981  
String name = arrData[1];  
//name is "Kim Victorya"  
String position = arrData[2];  
//position is "engineer"
```

```javascript
var strData = "1981|Kim Victorya|engineer";  
var arrData = strData.split("|");  
var year = parseInt(arrData[0]);  
//year is 1981  
var name = arrData[1];  
//name is "Kim Victorya"  
var position = arrData[2];  
//position is "engineer"
```

```cs
var strData = "1981|Kim Victorya|engineer";  
var arrData = strData.Split('|');  
var year = int.Parse(arrData[0]);  
//year is 1981  
var name = arrData[1];  
//name is "Kim Victorya"  
var position = arrData[2];  
//position is "engineer"
```

```go
var strData = "1981|Kim Victorya|engineer"  
var arrData = strings.Split(strData, "|")  
year, err := strconv.Atoi(arrData[0])  
_ = err  
//year is 1981  
var name = arrData[1]  
//name is "Kim Victorya"  
var position = arrData[2]  
//position is "engineer"
```
## Replace multiple characters

```python
import re  
  
s = "1-/[=2=/]-3"  
separators = "[=/\\[\\]]"  
arStr = re.split(separators, s)  
s = "".join(arStr)  
# s is "1-2-3"
```

```java
String str = "1-/[=2=/]-3";  
String separators = "[=/\\[]|]";  
String[] arStr = str.split(separators);  
str = String.join("", arStr);  
//str is "1-2-3"
```

```javascript
var str = "1-/[=2=/]-3";  
var separators = /[=/\\[]|]/;  
var arStr = str.split(separators);  
str = arStr.join("");  
//str is "1-2-3"
```

```cs
var str = "1-/[=2=/]-3";  
var separators = new [] {'/', '=', '[', ']'};  
var arStr = str.Split(separators, StringSplitOptions.RemoveEmptyEntries);  
str = string.Join("", arStr);  
//str is "1-2-3"
```

```go
var replacer = strings.NewReplacer(  
"[", "", "]", "", "=", "", "/", "")  
var str = "1-/[=2=/]-3"  
str = replacer.Replace(str)  
//str is "1-2-3"
```
## Empty strings
```python
# Empty strings  
someEmptyString = ""  
anotherEmptyString = str("")  
  
if not anotherEmptyString:  
print("string is empty")
```

```java
//Empty strings  
String someEmptyString = "";  
String anotherEmptyString = new String();  
  
if (anotherEmptyString.isEmpty()) {  
//string is empty  
}
```

```javascript
//Empty strings  
var someEmptyString = "";  
var anotherEmptyString = '';  
  
if (!anotherEmptyString) {  
//string is empty  
}
```

```cs
//Empty strings  
var someEmptyString = "";  
var anotherEmptyString = string.Empty;  
  
if (string.IsNullOrEmpty(anotherEmptyString)) {  
//string is empty or null  
}
```

```go
//Empty strings  
var someEmptyString = ""  
var anotherEmptyString = ""  
  
if someEmptyString == "" {  
//string is empty  
}  
  
if len(anotherEmptyString) == 0 {  
//string is empty  
}
```
## Getting substring
```python
s = "one way ticket"  
way = s[4: -7]  
# way is "way"
```

```java
String str = "one way ticket";  
String way = str.substring(4, 7);  
//way is "way"
```

```javascript
var str = "one way ticket";  
var way = str.substring(4, 7);  
//way is "way"
```

```cs
var str = "one way ticket";  
var way = str.Substring(4, 3);  
//way is "way"
```

```go
var str = "one way ticket"  
var way = str[4:7]  
//way is "way"
```
